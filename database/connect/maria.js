/*
const maria = require('mysql');

const conn = maria.createConnection({
    host: 'localhost',
    port: process.env.SERVER_PORT,
    user: 'user',
    password: 'pwd',
    database: 'db name'
});

module.exports = conn;*/